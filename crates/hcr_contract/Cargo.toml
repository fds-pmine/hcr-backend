[package]
name = "hcr_contract"
description = "hcr.v1 wire contract — shared by the HCR backend, the replay engine and device firmware."
edition.workspace = true
version.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[features]
default = ["std"]
std = ["serde/std", "serde_json/std"]

[lints.rust]
missing_docs = "warn"
